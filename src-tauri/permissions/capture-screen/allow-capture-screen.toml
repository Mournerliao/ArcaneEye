"$schema" = "schemas/schema.json"

[permission]
identifier = "allow-capture-screen"
description = "Capture the primary monitor screen and return a base64 PNG data URL."
commands.allow = ["capture_screen"]
